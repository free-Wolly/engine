export enum UserType {
  EMPLOYEE = 'employee',
  USER = 'user',
  CLIENT = 'client',
}

export enum UserTypee {
  employee,
  user,
  client,
}

export enum UserRole {
  ADMIN = 'ADMIN',
  MANAGER = 'MANAGER',
}
